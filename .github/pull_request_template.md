## Summary

What changed and why?

## Testing

- [ ] `python -m pytest -q`
- [ ] `npm run build` in `dashboard/` if frontend changed

## Privacy and Data Safety

- [ ] No `.env`, `credentials.json`, `token.json`, database files, local attachments, or Chroma indexes committed
- [ ] No real Gmail content, API keys, OAuth tokens, or private attachments included
- [ ] Any new external data flow is documented
